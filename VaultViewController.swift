//
//  VaultViewController.swift
//  SunCoin
//
//  Created by MACK FISCHER on 4/7/23.
//

import UIKit

public var numButtonClicks = 0
public var coinsFlipped = 0

public var gameScreenAccessed = 0
public var flipScreenAccessed = 0
public var interestScreenAccessed = 0
public var tipScreenAccessed = 0
public var vaultScreenAccessed = 0

class VaultViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
        title = "Vault"
        // Do any additional setup after loading the view.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
